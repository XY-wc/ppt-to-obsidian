# PPT2Obsidian · 主界面重设计

## 模式
Convention mode — 桌面生产力工具。优先层级、留白、状态完整、可扫读，不做品牌大片式冒险。

## 主体
把课程 PPT/PDF 转成 Obsidian 讲义笔记的本地工作台。用户要在 30 秒内完成「选课 → 选课件 → 生成 → 打开」。

## 色板
| Token | Hex | 用途 |
|-------|-----|------|
| canvas | #EEF0F6 | 窗口底 |
| sidebar | #F7F8FC | 左侧知识库栏 |
| surface | #FFFFFF | 卡片/面板 |
| surface-2 | #F8F9FD | 输入底、次级面 |
| ink | #171A2B | 主文字 |
| ink-med | #5A6178 | 次文字 |
| ink-light | #8B93A7 | 弱提示 |
| primary | #4F46E5 | 主操作/选中 |
| primary-hover | #4338CA | hover |
| primary-soft | #EEF0FF | 浅底/徽章 |
| success | #059669 | 成功 |
| danger | #DC2626 | 危险/错误 |
| border | #E2E6F0 | 描边 |
| log-bg | #12182A | 日志深底 |

## 字体
- 全部 `"Microsoft YaHei UI"`，Windows 原生可读
- 顶栏应用名 12 bold（紧凑，不是营销 H1）
- 区块标题 11 bold
- 正文 10 / 小字 9 / 微字 8
- 主按钮 12 bold；日志 `Consolas` 9

## 布局
```
┌──────────────────────────────────────────────────────┐
│ [icon] PPT → Obsidian   [状态]  [用户] [退出]        │  顶栏 48px
├──────────┬───────────────────────────────────────────┤
│ 知识库   │ ① 课件（拖入/已选） │
│ Vault 根 │ ② 专业 · 输出 · 模型 · 形态              │
│ 课程列表 │ ③ 生成（主按钮 + 进度 + 日志 + 结果）   │
│ 本课笔记 │ ④ 对话修订（卡片模式）                   │
├──────────┴───────────────────────────────────────────┤
│ 状态栏 28px：就绪/进行中 · 模型 · 输出路径          │
└──────────────────────────────────────────────────────┘
```
- 侧栏固定 280px：课程 = 知识库导航；笔记树 = 资源浏览
- 主区单列滚动：线性工作流，不再左右对半挤
- 间距节奏 4/8/12/16/20；区块间距 12；卡内边距 16

## 签名元素
左侧「知识库」栏（Vault 根 + 课程 + 本课笔记树）——让窗口像笔记工作台，而不是表单堆。

## 状态覆盖
- 空：无课程 / 未选课件 / 未生成 / 无笔记 → 给出下一步动作
- 进行：进度条 + 阶段文案 + 主按钮禁用
- 成功：绿状态 + 路径 + 打开按钮可用
- 失败：红状态 + 日志 err 标签

## 改动边界
保留全部业务方法名与数据流（课程 prefs、模型选择记忆、生成线程、对话修订、ModelDialog 逻辑）；只重做布局与视觉。
